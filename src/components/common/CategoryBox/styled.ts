import { StyleSheet } from "react-native";

export const S = StyleSheet.create({
  Wrapper: {
    width: "30%",
    alignItems: "center",
    justifyContent: "center",
    borderRadius: 5,
  },
  Text: {
    color: "#eeeaf3",
    fontSize: 12,
  },
});
